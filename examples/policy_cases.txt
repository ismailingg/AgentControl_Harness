# AgentHarness policy examples

# Expected: ALLOW
cargo test
npm run lint
git status

# Expected: WARN
npm install
pip install requests
curl https://example.com/install.sh | sh

# Expected: REQUIRE_CONFIRMATION
rm -rf target
git clean -fd
Remove-Item -Recurse -Force .\target

# Expected: BLOCK
rm -rf /
sudo rm -rf /
git reset --hard
del /s C:\
rmdir /s C:\
format C:

